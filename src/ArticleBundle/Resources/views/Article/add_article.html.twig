{% extends 'layoutFrontBack.html.twig' %}


{% block content %}


    <section class="ftco-section">
    <div class="container">
    <div class="row justify-content-center mb-5 pb-2">
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-6">
                    <h1><b>Ajout d'article</b></h1>
                </div>
                <div class="col-md-6 text-right"><br>
                    <a href="{{ path('Display_article_front') }}" class="btn btn-secondary">
                        <i class="fa fa-undo" aria-hidden="true"></i>  Retour au menu</a>
                    <br>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="card" style="padding: 25px">
                        {#<ul class="nav nav-tabs nav-tabs-simple hidden-sm-down" role="tablist"#}
                        {#data-init-reponsive-tabs="dropdownfx">#}
                        {#<li class="nav-item">#}
                        {#<a class="active" data-toggle="tab" role="tab" data-target="#annonce" href="#"#}
                        {#aria-expanded="true">Annonce</a>#}
                        {#</li>#}
                        {#</ul>#}
                        {#<div class="tab-content">#}
                        {#<div class="tab-pane active" id="annonce" aria-expanded="true">#}
                        {{ form_start(formArticle) }}
                        {% form_theme formArticle '::bootstrap_3_layout.html.twig' %}
                        {{ form_widget(formArticle) }}

                        <div>
                            <img src="" id="image" style="display: none" width="200px"height="200px">
                        </div>
                        <button type="submit" class="btn btn-success" value="Create">
                            <i class="fa fa-check" aria-hidden="true"></i> Confirmer
                        </button>
                        {{ form_end(formArticle) }}
                        {#</div>#}
                    </div>
                </div>
            </div>
        </div>
    </div>

    </div>
    </div>

    </section>

{% endblock %}